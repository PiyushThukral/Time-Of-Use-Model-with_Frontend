# config.py

CONSUMER_PATTERNS = [
    "consumer",
    "cons_no",
    "cons no",
    "cons",
    "consumer_no",
    "consumer_number",
    "consumerid",
    "consumer id",
    "consumer_code",
]

STANDARD_CONSUMER_COLUMN = "Consumer No"

MONTH_CANDIDATES = ["month"]  # case-insensitive match

CATEGORY_PATTERNS = ["category", "category_code", "category code", "categorycode", "category_name"]

CONNECTED_LOAD_CANDIDATES = [
    "connected_load",
    "connected load", 
    "sanctioned_load_kw", 
    "sanctioned load", 
    "sanctioned_load",
    "sanctioned_load_(kw)",
    "sanctionedload(kw)"]
